# Вправа по програмуванню 6-4
# Лічильник значень


# Головна функція.
def main():
    # Відкриття файлу для читання.
    infile = open(r"chapter_6\data\names.txt")

    # Первинне читання файлу.
    line = infile.readline()

    # Ініціалізація накопичувальної змінної.
    counter = 0

    # Читання файлу та підрахунок кількості рядків.
    while line != '':
        counter += 1

        line = infile.readline()

    # Виводимо результати на екран.
    print(f"Кількість прочитаних із файлу значень: {counter}")

    # Закриття файлу.
    infile.close()


# Виклик головної функції.
main()
